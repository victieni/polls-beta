export enum ePollStatus {
	DRAFT = "draft",
	ACTIVE = "active",
	CLOSED = "closed",
	OPEN = "open",
	ARCHIVED = "archived",
	INVALIDATED = "invalidated",
}

export enum ePollType {
	SIMPLE = "simple",
	ELECTION = "election",
	SURVEY = "survey",
}
